L2 expectedcode 제안

# L2-01

[추가]# 구구단 n단 출력하기
[추가]n=5
[추가]for i in range(1, 10):
[추가]    print(n, '*', i, '=', n * i)

# L2-02

# Tkinter 라이브러리 호출
import tkinter as tk

# 메인 윈도우 생성
main_win = tk.Tk()

# 메인 윈도우 이름 설정
main_win.title("DIY 계산기")

[수정]# 디스플레이 엔트리 생성 및 배치(column span 추가)
display_entry = tk.Entry(main_win, width=45, bg="light green")
[수정]display_entry.grid(row=0, column=0, columnspan=2)

[삭제]# 숫자1 버튼 생성 및 배치
[삭제]button1 = tk.Button(main_win, text="1", width=5)
[삭제]button1.grid(row=1, column=0, sticky='W')

[삭제]# 숫자2 버튼 생성 및 배치
[삭제]button2 = tk.Button(main_win, text="2", width=5)
[삭제]button2.grid(row=2, column=0, sticky='W')

[삭제]# 숫자3 버튼 생성 및 배치
[삭제]button3 = tk.Button(main_win, text="3", width=5)
[삭제]button3.grid(row=3, column=0, sticky='W')

[추가]# 번호 프레임 생성 및 배치
[추가]num_frame = tk.Frame(main_win)
[추가]num_frame.grid(row=1, column=0, sticky='W')

# 메인 윈도우 반복 명령
main_win.mainloop()

# L2-03

# Tkinter 라이브러리 호출
import tkinter as tk

# 메인 윈도우 생성
main_win = tk.Tk()

# 메인 윈도우 이름 설정
main_win.title("DIY 계산기")

# 디스플레이 엔트리 생성 및 배치(column span 추가)
display_entry = tk.Entry(main_win, width=45, bg="light green")
display_entry.grid(row=0, column=0, columnspan=2)

# 번호 프레임 생성 및 배치
num_frame = tk.Frame(main_win)
num_frame.grid(row=1, column=0, sticky='W')

[추가]# 넣고 싶은 번호 리스트(순서대로)
[추가]num_list = ['7', '8', '9',
            '4', '5', '6',
            '1', '2', '3',
            '0', '.', '=']

[추가]# 반복문에서 사용할 임시 변수
[추가]row_tmp = 0
[추가]col_tmp = 0

[추가]# num_text는 num_list 값을 순서대로 갖게 된다
[추가]for num_text in num_list:

[추가]    # Button을 생성하고, row_tmp, col_tmp 위치에 배치
[추가]    num_button = tk.Button(num_frame, text=num_text, width=5)
[추가]    num_button.grid(row=row_tmp, column=col_tmp)

[추가]    # 버튼을 배치했으면 다음 칸으로 이동하도록 col_tmp에 1 더한다
[추가]    col_tmp = col_tmp + 1
[추가]    # col_tmp가 2보다 크다면 다음 줄로 이동한다
[추가]    if col_tmp > 2:
[추가]        col_tmp = 0
[추가]        row_tmp = row_tmp + 1

# 메인 윈도우 반복 명령
main_win.mainloop()

# L2-04

# Tkinter 라이브러리 호출
import tkinter as tk

# 메인 윈도우 생성
main_win = tk.Tk()

# 메인 윈도우 이름 설정
main_win.title("DIY 계산기")

# 디스플레이 엔트리 생성 및 배치(column span 추가)
display_entry = tk.Entry(main_win, width=45, bg="light green")
display_entry.grid(row=0, column=0, columnspan=2)

# 번호 프레임 생성 및 배치
num_frame = tk.Frame(main_win)
num_frame.grid(row=1, column=0, sticky='W')

# 넣고 싶은 번호 리스트(순서대로)
num_list = ['7', '8', '9',
            '4', '5', '6',
            '1', '2', '3',
            '0', '.', '=']

# 반복문에서 사용할 임시 변수
row_tmp = 0
col_tmp = 0

# num_text는 num_list 값을 순서대로 갖게 된다
for num_text in num_list:

    # Button을 생성하고, row_tmp, col_tmp 위치에 배치
    num_button = tk.Button(num_frame, text=num_text, width=5)
    num_button.grid(row=row_tmp, column=col_tmp)

    # 버튼을 배치했으면 다음 칸으로 이동하도록 col_tmp에 1 더한다
    col_tmp = col_tmp + 1
    # col_tmp가 2보다 크다면 다음 줄로 이동한다
    if col_tmp > 2:
        col_tmp = 0
        row_tmp = row_tmp + 1

[추가]# 연산자 프레임 생성 및 배치
[추가]op_frame = tk.Frame(main_win)
[추가]op_frame.grid(row=1, column=1, sticky='E')

[추가]# 넣고 싶은 연산자 리스트(순서대로)
[추가]op_list = ['*', '/',
           '+', '-',
           '(', ')',
           'C', 'AC']

[추가]# 반복문에서 사용할 임시 변수
[추가]row_tmp = 0
[추가]col_tmp = 0

[추가]# op_text는 op_list 값을 순서대로 갖게 된다
[추가]for op_text in op_list:

[추가]    # Button을 생성하고, row_tmp, col_tmp 위치에 배치
[추가]    op_button = tk.Button(op_frame, text=op_text, width=5)
[추가]    op_button.grid(row=row_tmp, column=col_tmp)

[추가]    # 버튼을 배치했으면 다음 칸으로 이동하도록 col_tmp에 1 더한다
[추가]    col_tmp = col_tmp + 1
[추가]    # col_tmp가 1보다 크다면 다음 줄로 이동한다
[추가]    if col_tmp > 1:
[추가]        col_tmp = 0
[추가]        row_tmp = row_tmp + 1

# 메인 윈도우 반복 명령
main_win.mainloop()

# L2-05

# Tkinter 라이브러리 호출
import tkinter as tk

[추가]# 버튼 클릭시 실행 함수
[추가]def buttonClick(key):
[추가]    # 눌러진 버튼 텍스트 값을 디스플레이에 추가
[추가]    display_entry.insert(tk.END, key)

# 메인 윈도우 생성
main_win = tk.Tk()

# 메인 윈도우 이름 설정
main_win.title("DIY 계산기")

# 디스플레이 엔트리 생성 및 배치(column span 추가)
display_entry = tk.Entry(main_win, width=45, bg="light green")
display_entry.grid(row=0, column=0, columnspan=2)

# 번호 프레임 생성 및 배치
num_frame = tk.Frame(main_win)
num_frame.grid(row=1, column=0, sticky='W')

# 넣고 싶은 번호 리스트(순서대로)
num_list = ['7', '8', '9',
            '4', '5', '6',
            '1', '2', '3',
            '0', '.', '=']

# 반복문에서 사용할 임시 변수
row_tmp = 0
col_tmp = 0

# num_text는 num_list 값을 순서대로 갖게 된다
for num_text in num_list:

[추가]    # 버튼을 눌렀을 때 cmd_tmp가 실행된다
[추가]    def cmd_tmp(key_input=num_text):
[추가]        # num_text 값을 넘겨주면서 button_click을 호출
[추가]        button_click(key_input)

    # Button을 생성하고, row_tmp, col_tmp 위치에 배치
[수정]    num_button = tk.Button(num_frame, text=num_text, width=5, command=cmd_tmp)
    num_button.grid(row=row_tmp, column=col_tmp)

    # 버튼을 배치했으면 다음 칸으로 이동하도록 col_tmp에 1 더한다
    col_tmp = col_tmp + 1
    # col_tmp가 2보다 크다면 다음 줄로 이동한다
    if col_tmp > 2:
        col_tmp = 0
        row_tmp = row_tmp + 1

# 연산자 프레임 생성 및 배치
op_frame = tk.Frame(main_win)
op_frame.grid(row=1, column=1, sticky='E')

# 넣고 싶은 연산자 리스트(순서대로)
op_list = ['*', '/',
           '+', '-',
           '(', ')',
           'C', 'AC']

# 반복문에서 사용할 임시 변수
row_tmp = 0
col_tmp = 0

# op_text는 op_list 값을 순서대로 갖게 된다
for op_text in op_list:

[추가]    # 버튼을 눌렀을 때 cmd_tmp가 실행된다
[추가]    def cmd_tmp(key_input=op_text):
[추가]        # op_text 값을 넘겨주면서 button_click을 호출
[추가]        button_click(key_input)

    # Button을 생성하고, row_tmp, col_tmp 위치에 배치
[수정]    op_button = tk.Button(op_frame, text=op_text, width=5, command=cmd_tmp)
    op_button.grid(row=row_tmp, column=col_tmp)

    # 버튼을 배치했으면 다음 칸으로 이동하도록 col_tmp에 1 더한다
    col_tmp = col_tmp + 1
    # col_tmp가 1보다 크다면 다음 줄로 이동한다
    if col_tmp > 1:
        col_tmp = 0
        row_tmp = row_tmp + 1

# 메인 윈도우 반복 명령
main_win.mainloop()