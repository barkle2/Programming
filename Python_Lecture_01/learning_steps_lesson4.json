{
  "courseTitle": "[파이썬 강의 4] 계산기 만들기",
  "lesson": {
    "id": "lesson-4",
    "title": "클립보드 기능 추가하기",
    "goal": "funcClick 함수와 클립보드 UI를 추가하고, 펑션키와 백스페이스 키를 연결해 계산기를 완성한다.",
    "sourceFile": "Calc_Lesson4.py",
    "estimatedMinutes": 45,
    "mode": "full-program"
  },
  "gameRules": {
    "successStreakMilestones": [3, 5, 7],
    "typoBadgeMilestones": [
      { "count": 3, "badge": "불굴의 의지 I" },
      { "count": 7, "badge": "불굴의 의지 II" },
      { "count": 15, "badge": "불굴의 의지 III" }
    ]
  },
  "steps": [
    {
      "id": "L4-01",
      "title": "funcClick 함수와 클립보드 UI 추가하기",
      "concepts": ["funcClick", "Frame", "클립보드", "F1~F6", "cmd_F1"],
      "explanation": "L3에서 완성한 계산기에 계산 결과를 임시 저장할 수 있는 클립보드 기능을 추가한다.\n\n`funcClick` 함수는 F1~F6 키에 반응해 클립보드1~3에 값을 저장하거나 불러온다. F1을 누르면 display_entry의 내용을 clip1_entry에 복사하고, F4를 누르면 clip1_entry의 내용을 다시 display_entry에 붙여넣는다.\n\n클립보드 UI는 새로운 Frame인 `clip_frame`에 담긴다. 각 행에는 저장 버튼(F1/F2/F3), 값을 보여주는 Entry, 붙여넣기 버튼(F4/F5/F6)이 배치된다. 버튼을 클릭하면 `cmd_F1`~`cmd_F6` 함수가 호출되고, 이 함수가 다시 `funcClick`을 호출하는 구조다.\n\n`clip_frame`은 `row=2`에 배치해 번호/연산자 프레임 아래에 나타나도록 한다.",
      "mission": "L3-05 코드에 funcClick 함수를 buttonClick 아래에 추가하고, op_frame 루프 아래에 clip_frame과 클립보드1~3 버튼/엔트리를 추가해 전체 프로그램을 입력한다.",
      "expectedCode": "# Tkinter 라이브러리 호출\nimport tkinter as tk\n\n# Time 라이브러리 호출\nimport time\n\n# 넣고 싶은 번호 리스트(순서대로)\nnum_list = ['7', '8', '9',\n            '4', '5', '6',\n            '1', '2', '3',\n            '0', '.', '=']\n\n# 넣고 싶은 연산자 리스트(순서대로)\nop_list = ['*', '/',\n           '+', '-',\n           '(', ')',\n           'C', 'AC']\n\n# num_list와 op_list를 합친 key_list 생성\nkey_list = num_list + op_list\n# key_list에서 'AC', 'C', '='를 제거\nkey_list.remove('AC')\nkey_list.remove('C')\nkey_list.remove('=')\n\n# 키보드 입력시 실행 함수\ndef inputKey(key):\n\n    # display_entry를 입력가능한 상태로 전환\n    display_entry.configure(state=tk.NORMAL)\n\n    # 입력된 키가 '=' 또는 엔터인 경우\n    if key.char == '=' or key.char == '\\r':\n        try:\n            # display_entry 수식을 계산\n            result = str(round(eval(display_entry.get()), 2))\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # display_entry에 결과값 출력\n            display_entry.insert(tk.END, result)\n        # 계산이 되지 않는 수식인 경우 1초 동안 '오류' 표시\n        except:\n            # 현재 display_entry 수식을 임시로 저장\n            result_tmp = display_entry.get()\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 안내 메시지 표출\n            display_entry.insert(0, \"계산할 수 없는 수식입니다\")\n            display_entry.update()\n            # 1초간 정지\n            time.sleep(1)\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 임시로 저장해두었던 수식 다시 보여주기\n            display_entry.insert(0, result_tmp)\n    # 입력된 키가 'C' 또는 'c'인 경우\n    elif key.char == 'C' or key.char == 'c':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n    # 입력된 키가 'A' 또는 'a'인 경우\n    elif key.char == 'A' or key.char == 'a':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n    # 입력된 키가 '=', '\\r', 'C', 'c', 'A', 'a' 외의 키를 누른 경우\n    elif key.char in key_list:\n        # display_entry에 입력된 키 값을 추가\n        display_entry.insert(tk.END, key.char)\n\n    # display_entry를 입력불가능한 상태로 전환\n    display_entry.configure(state=\"readonly\")\n\n# 버튼 클릭시 실행 함수\ndef buttonClick(key):\n\n    # display_entry를 입력가능한 상태로 전환\n    display_entry.configure(state=tk.NORMAL)\n\n    # '=' 버튼을 누른 경우\n    if key == '=':\n        try:\n            # display_entry 수식을 계산\n            result = str(round(eval(display_entry.get()), 2))\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # display_entry에 결과값 출력\n            display_entry.insert(tk.END, result)\n        # 계산이 되지 않는 수식인 경우 1초 동안 '오류' 표시\n        except:\n            # 현재 display_entry 수식을 임시로 저장\n            result_tmp = display_entry.get()\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 안내 메시지 표출\n            display_entry.insert(0, \"계산할 수 없는 수식입니다\")\n            display_entry.update()\n            # 1초간 정지\n            time.sleep(1)\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 임시로 저장해두었던 수식 다시 보여주기\n            display_entry.insert(0, result_tmp)\n    # 'C' 버튼을 누른 경우\n    elif key == 'C':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n    # 'AC' 버튼을 누른 경우\n    elif key == 'AC':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n    # '=', 'C', 'AC' 외의 버튼을 누른 경우\n    else:\n        # 눌러진 버튼 텍스트 값을 디스플레이에 추가\n        display_entry.insert(tk.END, key)\n\n    # display_entry를 입력불가능한 상태로 전환\n    display_entry.configure(state=\"readonly\")\n\n# 펑션키 입력 함수\ndef funcClick(key):\n    # display_entry를 입력가능한 상태로 전환\n    display_entry.configure(state=tk.NORMAL)\n\n    # F1 키를 누른 경우\n    if key == \"F1\":\n        # 클립보드1 내용 삭제\n        clip1_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드1로 복사\n        clip1_entry.insert(tk.END, display_entry.get())\n    # F2 키를 누른 경우\n    elif key == \"F2\":\n        # 클립보드2 내용 삭제\n        clip2_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드2로 복사\n        clip2_entry.insert(tk.END, display_entry.get())\n    # F3 키를 누른 경우\n    elif key == \"F3\":\n        # 클립보드3 내용 삭제\n        clip3_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드3로 복사\n        clip3_entry.insert(tk.END, display_entry.get())\n    # F4 키를 누른 경우\n    elif key == \"F4\":\n        # 클립보드1 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip1_entry.get())\n        # 클립보드1 내용 삭제\n        clip1_entry.delete(0, tk.END)\n    # F5 키를 누른 경우\n    elif key == \"F5\":\n        # 클립보드2 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip2_entry.get())\n        # 클립보드2 내용 삭제\n        clip2_entry.delete(0, tk.END)\n    # F6 키를 누른 경우\n    elif key == \"F6\":\n        # 클립보드3 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip3_entry.get())\n        # 클립보드3 내용 삭제\n        clip3_entry.delete(0, tk.END)\n\n    # display_entry를 입력불가능한 상태로 전환\n    display_entry.configure(state=\"readonly\")\n\n# 메인 윈도우 생성\nmain_win = tk.Tk()\n\n# 메인 윈도우 이름 설정\nmain_win.title(\"DIY 계산기\")\n\n# 메인 윈도우를 항상 위로\nmain_win.attributes(\"-topmost\", True)\n\n# focus-on 상태에서 키를 누르면 inputKey 함수 실행\nmain_win.bind(\"<Key>\", inputKey)\n\n# 디스플레이 엔트리 생성 및 배치(column span 추가)\ndisplay_entry = tk.Entry(main_win, width=45, bg=\"light green\")\ndisplay_entry.grid(row=0, column=0, columnspan=2)\n\n# display_entry를 입력불가능한 상태로 전환\ndisplay_entry.configure(state=\"readonly\")\n\n# 번호 프레임 생성 및 배치\nnum_frame = tk.Frame(main_win)\nnum_frame.grid(row=1, column=0, sticky='W')\n\n# 반복문에서 사용할 임시 변수\nrow_tmp = 0\ncol_tmp = 0\n\n# num_text는 num_list 값을 순서대로 갖게 된다\nfor num_text in num_list:\n\n    # 버튼을 눌렀을 때 cmd_tmp가 실행된다\n    def cmd_tmp(key_input=num_text):\n        # num_text 값을 넘겨주면서 button_click을 호출\n        button_click(key_input)\n\n    # Button을 생성하고, row_tmp, col_tmp 위치에 배치\n    num_button = tk.Button(num_frame, text=num_text, width=5, command=cmd_tmp)\n    num_button.grid(row=row_tmp, column=col_tmp)\n\n    # 버튼을 배치했으면 다음 칸으로 이동하도록 col_tmp에 1 더한다\n    col_tmp = col_tmp + 1\n    # col_tmp가 2보다 크다면 다음 줄로 이동한다\n    if col_tmp > 2:\n        col_tmp = 0\n        row_tmp = row_tmp + 1\n\n# 연산자 프레임 생성 및 배치\nop_frame = tk.Frame(main_win)\nop_frame.grid(row=1, column=1, sticky='E')\n\n# 반복문에서 사용할 임시 변수\nrow_tmp = 0\ncol_tmp = 0\n\n# op_text는 op_list 값을 순서대로 갖게 된다\nfor op_text in op_list:\n\n    # 버튼을 눌렀을 때 cmd_tmp가 실행된다\n    def cmd_tmp(key_input=op_text):\n        # op_text 값을 넘겨주면서 button_click을 호출\n        button_click(key_input)\n\n    # Button을 생성하고, row_tmp, col_tmp 위치에 배치\n    op_button = tk.Button(op_frame, text=op_text, width=5, command=cmd_tmp)\n    op_button.grid(row=row_tmp, column=col_tmp)\n\n    # 버튼을 배치했으면 다음 칸으로 이동하도록 col_tmp에 1 더한다\n    col_tmp = col_tmp + 1\n    # col_tmp가 1보다 크다면 다음 줄로 이동한다\n    if col_tmp > 1:\n        col_tmp = 0\n        row_tmp = row_tmp + 1\n\n# 클립보드 프레임 생성 및 배치\nclip_frame = tk.Frame(main_win)\nclip_frame.grid(row=2, column=0, columnspan=2, sticky='N')\n\n# F1~F6를 눌렀을 때 실행되는 함수\ndef cmd_F1():\n    # 단순히 funcClick을 호출\n    funcClick('F1')\n\ndef cmd_F2():\n    # 단순히 funcClick을 호출\n    funcClick('F2')\n\ndef cmd_F3():\n    # 단순히 funcClick을 호출\n    funcClick('F3')\n\ndef cmd_F4():\n    # 단순히 funcClick을 호출\n    funcClick('F4')\n\ndef cmd_F5():\n    # 단순히 funcClick을 호출\n    funcClick('F5')\n\ndef cmd_F6():\n    # 단순히 funcClick을 호출\n    funcClick('F6')\n\n# 클립보드1 입출력 버튼, 엔트리 생성 및 배치\nclip1_input_btn = tk.Button(clip_frame, width=2, text='F1', command=cmd_F1)\nclip1_input_btn.grid(row=0, column=0)\nclip1_entry = tk.Entry(clip_frame, width=20, bg=\"light pink\")\nclip1_entry.grid(row=0, column=1)\nclip1_output_btn = tk.Button(clip_frame, width=2, text='F4', command=cmd_F4)\nclip1_output_btn.grid(row=0, column=2)\n\n# 클립보드2 입출력 버튼, 엔트리 생성 및 배치\nclip2_input_btn = tk.Button(clip_frame, width=2, text='F2', command=cmd_F2)\nclip2_input_btn.grid(row=1, column=0)\nclip2_entry = tk.Entry(clip_frame, width=20, bg=\"light blue\")\nclip2_entry.grid(row=1, column=1)\nclip2_output_btn = tk.Button(clip_frame, width=2, text='F5', command=cmd_F5)\nclip2_output_btn.grid(row=1, column=2)\n\n# 클립보드3 입출력 버튼, 엔트리 생성 및 배치\nclip3_input_btn = tk.Button(clip_frame, width=2, text='F3', command=cmd_F3)\nclip3_input_btn.grid(row=2, column=0)\nclip3_entry = tk.Entry(clip_frame, width=20, bg=\"light yellow\")\nclip3_entry.grid(row=2, column=1)\nclip3_output_btn = tk.Button(clip_frame, width=2, text='F6', command=cmd_F6)\nclip3_output_btn.grid(row=2, column=2)\n\n# 메인 윈도우 반복 명령\nmain_win.mainloop()",
      "validation": {
        "type": "multi-line-exact",
        "allowTrim": true,
        "allowExtraBlankLines": true,
        "editMode": "modify"
      },
      "commonErrors": [
        {
          "pattern": "funcClick(key_input)",
          "type": "NameError",
          "message": "funcClick은 'F1'처럼 문자열 키 이름을 받는다. cmd_F1 안에서는 `funcClick('F1')`처럼 직접 전달해야 한다."
        },
        {
          "pattern": "clip_frame.grid(row=1, column=0)",
          "type": "LayoutError",
          "message": "clip_frame은 번호/연산자 프레임 아래 새 행에 배치해야 한다. `row=2`를 사용하고 `columnspan=2`로 두 열에 걸쳐 배치한다."
        },
        {
          "pattern": "clip1_entry = tk.Entry(clip_frame, width=20, bg=\"light pink\")\nclip1_entry.grid(row=0, column=0)",
          "type": "LayoutError",
          "message": "clip1_entry는 F1 버튼 오른쪽에 배치한다. `column=1`을 사용해야 한다."
        }
      ],
      "successMessage": "클립보드 UI가 추가됐어요! F1~F3 버튼으로 저장하고, F4~F6 버튼으로 불러올 수 있습니다.",
      "mascot": "고드래곤_06.png",
      "mascotSpeech": "클립보드가 생겼어! F1을 누르면 계산 결과가 분홍 칸에 저장되고, F4를 누르면 다시 불러와. 복잡한 계산을 할 때 중간 결과를 저장해두고 나중에 쓸 수 있어서 진짜 편리하거든! 세 개나 있으니까 동시에 여러 값을 저장할 수도 있지~"
    },
    {
      "id": "L4-02",
      "title": "inputKey 개선과 AC 기능 완성, 창 이름 변경",
      "concepts": ["if/elif 순서", "keysym", "AC 전체 지우기", "readonlybackground", "리팩터링"],
      "explanation": "이번 단계에서는 세 가지를 개선한다.\n\n첫째, `inputKey` 함수의 조건 순서를 바꾼다. 이전에는 '='와 엔터를 먼저 검사했지만, 이제 `key_list`에 있는 일반 키를 가장 먼저 처리한다. 가장 자주 입력되는 키를 앞에 두면 코드 흐름이 더 자연스럽다.\n\n둘째, `inputKey`와 `buttonClick`의 'A'/'AC' 처리를 완성한다. 'A' 또는 'a' 키, 그리고 'AC' 버튼을 누르면 display_entry뿐만 아니라 clip1/2/3_entry도 함께 지운다.\n\n셋째, `inputKey` 안에 `key.keysym`을 사용하는 F1~F6 처리 블록을 추가한다. `key.char`는 일반 문자 키에 사용하고, 펑션키처럼 특수 키는 `key.keysym`으로 구분한다. 두 블록은 독립적인 `if`로 시작해 항상 검사된다.\n\n마지막으로 display_entry 너비를 35로, 버튼 너비를 3으로 줄여 화면을 콤팩트하게 만든다. `readonlybackground`를 추가하면 readonly 상태에서도 배경색이 유지된다.",
      "mission": "inputKey 조건 순서 변경, A/AC 클립 지우기 추가, F1~F6 keysym 블록 추가, display_entry·버튼 크기 변경을 반영해 전체 프로그램을 입력한다.",
      "expectedCode": "# Tkinter 라이브러리 호출\nimport tkinter as tk\n# Time 라이브러리 호출\nimport time\n\n# 넣고 싶은 번호 리스트(순서대로)\nnum_list = ['7', '8', '9',\n            '4', '5', '6',\n            '1', '2', '3',\n            '0', '.', '=']\n\n# 넣고 싶은 연산자 리스트(순서대로)\nop_list = ['*', '/',\n           '+', '-',\n           '(', ')',\n           'C', 'AC']\n\n# num_list와 op_list를 합친 key_list 생성\nkey_list = num_list + op_list\n# key_list에서 'AC', 'C', '='를 제거\nkey_list.remove('AC')\nkey_list.remove('C')\nkey_list.remove('=')\n\n# 키보드 입력시 실행 함수\ndef inputKey(key):\n    # display_entry를 입력가능한 상태로 전환\n    display_entry.configure(state=tk.NORMAL)\n\n    # 입력된 키가 key_list에 있는 키인 경우\n    if key.char in key_list:\n        # display_entry에 입력된 키 값을 추가\n        display_entry.insert(tk.END, key.char)\n    # 입력된 키가 '=' 또는 엔터인 경우\n    elif key.char == '=' or key.char == '\\r':\n        try:\n            # display_entry 수식을 계산\n            result = str(round(eval(display_entry.get()), 2))\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # display_entry에 결과값 출력\n            display_entry.insert(tk.END, result)\n        # 계산이 되지 않는 수식인 경우 1초 동안 '오류' 표시\n        except:\n            # 현재 display_entry 수식을 임시로 저장\n            result_tmp = display_entry.get()\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 안내 메시지 표출\n            display_entry.insert(0, \"계산할 수 없는 수식입니다\")\n            display_entry.update()\n            # 1초간 정지\n            time.sleep(1)\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 임시로 저장해두었던 수식 다시 보여주기\n            display_entry.insert(0, result_tmp)\n    # 입력된 키가 'C'인 경우\n    elif key.char == 'C' or key.char == 'c':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n    # 입력된 키가 'A'인 경우\n    elif key.char == 'A' or key.char == 'a':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n        # clip1,2,3_entry 내용 삭제\n        clip1_entry.delete(0, tk.END)\n        clip2_entry.delete(0, tk.END)\n        clip3_entry.delete(0, tk.END)\n\n    # F1 키를 누른 경우\n    if key.keysym == \"F1\":\n        # 클립보드1 내용 삭제\n        clip1_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드1로 복사\n        clip1_entry.insert(tk.END, display_entry.get())\n    # F2 키를 누른 경우\n    elif key.keysym == \"F2\":\n        # 클립보드2 내용 삭제\n        clip2_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드2로 복사\n        clip2_entry.insert(tk.END, display_entry.get())\n    # F3 키를 누른 경우\n    elif key.keysym == \"F3\":\n        # 클립보드3 내용 삭제\n        clip3_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드3로 복사\n        clip3_entry.insert(tk.END, display_entry.get())\n    # F4 키를 누른 경우\n    elif key.keysym == \"F4\":\n        # 클립보드1 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip1_entry.get())\n        # 클립보드1 내용 삭제\n        clip1_entry.delete(0, tk.END)\n    # F5 키를 누른 경우\n    elif key.keysym == \"F5\":\n        # 클립보드2 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip2_entry.get())\n        # 클립보드2 내용 삭제\n        clip2_entry.delete(0, tk.END)\n    # F6 키를 누른 경우\n    elif key.keysym == \"F6\":\n        # 클립보드3 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip3_entry.get())\n        # 클립보드3 내용 삭제\n        clip3_entry.delete(0, tk.END)\n\n    # display_entry를 입력불가능한 상태로 전환\n    display_entry.configure(state=\"readonly\")\n\n# 버튼 클릭시 실행 함수\ndef buttonClick(key):\n    # display_entry를 입력가능한 상태로 전환\n    display_entry.configure(state=tk.NORMAL)\n\n    # '=' 버튼을 누른 경우\n    if key == '=':\n        try:\n            # display_entry 수식을 계산\n            result = str(round(eval(display_entry.get()), 2))\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # display_entry에 결과값 출력\n            display_entry.insert(tk.END, result)\n        # 계산이 되지 않는 수식인 경우 1초 동안 '오류' 표시\n        except:\n            # 현재 display_entry 수식을 임시로 저장\n            result_tmp = display_entry.get()\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 안내 메시지 표출\n            display_entry.insert(0, \"계산할 수 없는 수식입니다\")\n            display_entry.update()\n            # 1초간 정지\n            time.sleep(1)\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 임시로 저장해두었던 수식 다시 보여주기\n            display_entry.insert(0, result_tmp)\n    # 'C' 버튼을 누른 경우\n    elif key == 'C':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n    # 'AC' 버튼을 누른 경우\n    elif key == 'AC':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n        # clip1,2,3_entry 내용 삭제\n        clip1_entry.delete(0, tk.END)\n        clip2_entry.delete(0, tk.END)\n        clip3_entry.delete(0, tk.END)\n    # '=', 'C', 'AC' 외의 버튼을 누른 경우\n    else:\n        # 눌러진 버튼 텍스트 값을 디스플레이에 추가\n        display_entry.insert(tk.END, key)\n\n    # display_entry를 입력불가능한 상태로 전환\n    display_entry.configure(state=\"readonly\")\n\n# 펑션키 입력 함수\ndef funcClick(key):\n    # display_entry를 입력가능한 상태로 전환\n    display_entry.configure(state=tk.NORMAL)\n\n    # F1 키를 누른 경우\n    if key == \"F1\":\n        # 클립보드1 내용 삭제\n        clip1_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드1로 복사\n        clip1_entry.insert(tk.END, display_entry.get())\n    # F2 키를 누른 경우\n    elif key == \"F2\":\n        # 클립보드2 내용 삭제\n        clip2_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드2로 복사\n        clip2_entry.insert(tk.END, display_entry.get())\n    # F3 키를 누른 경우\n    elif key == \"F3\":\n        # 클립보드3 내용 삭제\n        clip3_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드3로 복사\n        clip3_entry.insert(tk.END, display_entry.get())\n    # F4 키를 누른 경우\n    elif key == \"F4\":\n        # 클립보드1 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip1_entry.get())\n        # 클립보드1 내용 삭제\n        clip1_entry.delete(0, tk.END)\n    # F5 키를 누른 경우\n    elif key == \"F5\":\n        # 클립보드2 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip2_entry.get())\n        # 클립보드2 내용 삭제\n        clip2_entry.delete(0, tk.END)\n    # F6 키를 누른 경우\n    elif key == \"F6\":\n        # 클립보드3 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip3_entry.get())\n        # 클립보드3 내용 삭제\n        clip3_entry.delete(0, tk.END)\n\n\n    # display_entry를 입력불가능한 상태로 전환\n    display_entry.configure(state=\"readonly\")\n\n# 메인 윈도우 생성\nmain_win = tk.Tk()\n# 메인 윈도우 이름 설정\nmain_win.title(\"DIY 계산기\")\n# 메인 윈도우를 항상 위로\nmain_win.attributes(\"-topmost\", True)\n# focus-on 상태에서 키를 누르면 inputKey 함수 실행\nmain_win.bind(\"<Key>\", inputKey)\n\n# 디스플레이 엔트리 생성 및 배치\n# columnspan=2 추가\ndisplay_entry = tk.Entry(main_win, width=35, readonlybackground=\"light green\", bg=\"light green\")\ndisplay_entry.grid(row=0, column=0, columnspan=2)\n\n# display_entry를 입력불가능한 상태로 전환\ndisplay_entry.configure(state=\"readonly\")\n\n# 번호 프레임 생성 및 배치\nnum_frame = tk.Frame(main_win)\nnum_frame.grid(row=1, column=0, sticky='W')\n\n# 반복문에서 사용할 임시 변수\nrow_tmp = 0\ncol_tmp = 0\n\n# num_text는 num_list 값을 순서대로 갖게 된다\nfor num_text in num_list:\n\n    # 버튼을 눌렀을 때 cmd_tmp가 실행된다\n    def cmd_tmp(key_input=num_text):\n        # num_text 값을 넘겨주면서 button_click을 호출\n        buttonClick(key_input)\n\n    # Button을 생성하고, row_tmp, col_tmp 위치에 배치\n    num_button = tk.Button(num_frame, text=num_text, width=3, command=cmd_tmp)\n    num_button.grid(row=row_tmp, column=col_tmp)\n\n    # 버튼을 배치했으면 다음 칸으로 이동하도록 col_tmp에 1 더한다\n    col_tmp = col_tmp + 1\n    # col_tmp가 2보다 크다면 다음 줄로 이동한다\n    if col_tmp > 2:\n        col_tmp = 0\n        row_tmp = row_tmp + 1\n\n# 연산자 프레임 생성 및 배치\nop_frame = tk.Frame(main_win)\nop_frame.grid(row=1, column=1, sticky='E')\n\n# 반복문에서 사용할 임시 변수\nrow_tmp = 0\ncol_tmp = 0\n\n# op_text는 op_list 값을 순서대로 갖게 된다\nfor op_text in op_list:\n\n    # 버튼을 눌렀을 때 cmd_tmp가 실행된다\n    def cmd_tmp(key_input=op_text):\n        # op_text 값을 넘겨주면서 button_click을 호출\n        buttonClick(key_input)\n\n    # Button을 생성하고, row_tmp, col_tmp 위치에 배치\n    op_button = tk.Button(op_frame, text=op_text, width=3, command=cmd_tmp)\n    op_button.grid(row=row_tmp, column=col_tmp)\n\n    # 버튼을 배치했으면 다음 칸으로 이동하도록 col_tmp에 1 더한다\n    col_tmp = col_tmp + 1\n    # col_tmp가 1보다 크다면 다음 줄로 이동한다\n    if col_tmp > 1:\n        col_tmp = 0\n        row_tmp = row_tmp + 1\n\n# 클립보드 프레임 생성 및 배치\nclip_frame = tk.Frame(main_win)\nclip_frame.grid(row=2, column=0, columnspan=2, sticky='N')\n\n# F1~F6를 눌렀을 때 실행되는 함수\ndef cmd_F1():\n    # 단순히 funcClick을 호출\n    funcClick('F1')\n\ndef cmd_F2():\n    # 단순히 funcClick을 호출\n    funcClick('F2')\n\ndef cmd_F3():\n    # 단순히 funcClick을 호출\n    funcClick('F3')\n\ndef cmd_F4():\n    # 단순히 funcClick을 호출\n    funcClick('F4')\n\ndef cmd_F5():\n    # 단순히 funcClick을 호출\n    funcClick('F5')\n\ndef cmd_F6():\n    # 단순히 funcClick을 호출\n    funcClick('F6')\n\n# 클립보드1 입출력 버튼, 엔트리 생성 및 배치\nclip1_input_btn = tk.Button(clip_frame, width=2, text='F1', command=cmd_F1)\nclip1_input_btn.grid(row=0, column=0)\nclip1_entry = tk.Entry(clip_frame, width=20, bg=\"light pink\")\nclip1_entry.grid(row=0, column=1)\nclip1_output_btn = tk.Button(clip_frame, width=2, text='F4', command=cmd_F4)\nclip1_output_btn.grid(row=0, column=2)\n\n# 클립보드2 입출력 버튼, 엔트리 생성 및 배치\nclip2_input_btn = tk.Button(clip_frame, width=2, text='F2', command=cmd_F2)\nclip2_input_btn.grid(row=1, column=0)\nclip2_entry = tk.Entry(clip_frame, width=20, bg=\"light blue\")\nclip2_entry.grid(row=1, column=1)\nclip2_output_btn = tk.Button(clip_frame, width=2, text='F5', command=cmd_F5)\nclip2_output_btn.grid(row=1, column=2)\n\n# 클립보드3 입출력 버튼, 엔트리 생성 및 배치\nclip3_input_btn = tk.Button(clip_frame, width=2, text='F3', command=cmd_F3)\nclip3_input_btn.grid(row=2, column=0)\nclip3_entry = tk.Entry(clip_frame, width=20, bg=\"light yellow\")\nclip3_entry.grid(row=2, column=1)\nclip3_output_btn = tk.Button(clip_frame, width=2, text='F6', command=cmd_F6)\nclip3_output_btn.grid(row=2, column=2)\n\n# 메인 윈도우 반복 명령\nmain_win.mainloop()",
      "validation": {
        "type": "multi-line-exact",
        "allowTrim": true,
        "allowExtraBlankLines": true,
        "editMode": "modify"
      },
      "commonErrors": [
        {
          "pattern": "if key.keysym == \"F1\":\nelif key.keysym == \"F2\":",
          "type": "LogicError",
          "message": "F1~F6 keysym 블록은 char 블록과 독립적인 별도의 `if`로 시작해야 한다. `elif`로 시작하면 char 조건 중 하나가 실행된 경우 F키 블록이 무시된다."
        },
        {
          "pattern": "key.char == \"F1\"",
          "type": "AttributeError",
          "message": "펑션키는 `key.char`로 감지되지 않는다. `key.keysym == \"F1\"`을 사용해야 한다."
        },
        {
          "pattern": "display_entry = tk.Entry(main_win, width=35, bg=\"light green\")",
          "type": "MissingOption",
          "message": "readonly 상태에서도 배경색을 유지하려면 `readonlybackground=\"light green\"`을 추가해야 한다."
        }
      ],
      "successMessage": "키보드 F키로도 클립보드를 쓸 수 있게 됐어요! AC 버튼이 클립보드까지 한 번에 지웁니다.",
      "mascot": "고드래곤_09.png",
      "mascotSpeech": "key.char는 일반 문자 키에, key.keysym은 F1처럼 특수 키에 써야 해! 그리고 keysym 블록을 elif로 이으면 char 조건이 먼저 걸렸을 때 F키가 무시돼버려. 독립된 if로 따로 시작해야 항상 검사되거든. 작은 차이지만 동작이 완전히 달라지니까 꼭 기억해~"
    },
    {
      "id": "L4-03",
      "title": "백스페이스 키로 마지막 글자 지우기",
      "concepts": ["BackSpace", "keysym", "len()", "delete()", "문자열 길이"],
      "explanation": "`BackSpace` 키를 누르면 입력창의 마지막 글자 하나를 지우는 기능을 추가한다.\n\n`key.keysym == \"BackSpace\"`로 백스페이스 키를 감지한다. `len(display_entry.get())`으로 현재 입력창의 글자 수를 구하고, `display_entry.delete(display_len-1, tk.END)`로 마지막 글자를 지운다. `delete(시작, 끝)` 메서드는 시작 위치부터 끝까지를 지운다. 인덱스는 0부터 시작하므로 마지막 글자의 위치는 `글자수 - 1`이다.\n\n백스페이스 처리는 F1~F6 keysym 블록과 마찬가지로 char 블록과 독립적인 별도의 `if`로 시작한다. F1~F6 블록 바로 앞에 위치시킨다.",
      "mission": "inputKey 함수 안의 elif char 블록과 F1~F6 keysym 블록 사이에 BackSpace 처리 if 블록을 추가해 전체 프로그램을 입력한다.",
      "expectedCode": "# Tkinter 라이브러리 호출\nimport tkinter as tk\n# Time 라이브러리 호출\nimport time\n\n# 넣고 싶은 번호 리스트(순서대로)\nnum_list = ['7', '8', '9',\n            '4', '5', '6',\n            '1', '2', '3',\n            '0', '.', '=']\n\n# 넣고 싶은 연산자 리스트(순서대로)\nop_list = ['*', '/',\n           '+', '-',\n           '(', ')',\n           'C', 'AC']\n\n# num_list와 op_list를 합친 key_list 생성\nkey_list = num_list + op_list\n# key_list에서 'AC', 'C', '='를 제거\nkey_list.remove('AC')\nkey_list.remove('C')\nkey_list.remove('=')\n\n# 키보드 입력시 실행 함수\ndef inputKey(key):\n    # display_entry를 입력가능한 상태로 전환\n    display_entry.configure(state=tk.NORMAL)\n\n    # 입력된 키가 key_list에 있는 키인 경우\n    if key.char in key_list:\n        # display_entry에 입력된 키 값을 추가\n        display_entry.insert(tk.END, key.char)\n    # 입력된 키가 '=' 또는 엔터인 경우\n    elif key.char == '=' or key.char == '\\r':\n        try:\n            # display_entry 수식을 계산\n            result = str(round(eval(display_entry.get()), 2))\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # display_entry에 결과값 출력\n            display_entry.insert(tk.END, result)\n        # 계산이 되지 않는 수식인 경우 1초 동안 '오류' 표시\n        except:\n            # 현재 display_entry 수식을 임시로 저장\n            result_tmp = display_entry.get()\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 안내 메시지 표출\n            display_entry.insert(0, \"계산할 수 없는 수식입니다\")\n            display_entry.update()\n            # 1초간 정지\n            time.sleep(1)\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 임시로 저장해두었던 수식 다시 보여주기\n            display_entry.insert(0, result_tmp)\n    # 입력된 키가 'C'인 경우\n    elif key.char == 'C' or key.char == 'c':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n    # 입력된 키가 'A'인 경우\n    elif key.char == 'A' or key.char == 'a':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n        # clip1,2,3_entry 내용 삭제\n        clip1_entry.delete(0, tk.END)\n        clip2_entry.delete(0, tk.END)\n        clip3_entry.delete(0, tk.END)\n\n    # Backspace 키를 누른 경우\n    if key.keysym == \"BackSpace\":\n        # 현재 display_entry 글자수를 구해서\n        display_len = len(display_entry.get())\n        # 마지막 글자를 지운다\n        display_entry.delete(display_len-1, tk.END)\n\n    # F1 키를 누른 경우\n    if key.keysym == \"F1\":\n        # 클립보드1 내용 삭제\n        clip1_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드1로 복사\n        clip1_entry.insert(tk.END, display_entry.get())\n    # F2 키를 누른 경우\n    elif key.keysym == \"F2\":\n        # 클립보드2 내용 삭제\n        clip2_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드2로 복사\n        clip2_entry.insert(tk.END, display_entry.get())\n    # F3 키를 누른 경우\n    elif key.keysym == \"F3\":\n        # 클립보드3 내용 삭제\n        clip3_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드3로 복사\n        clip3_entry.insert(tk.END, display_entry.get())\n    # F4 키를 누른 경우\n    elif key.keysym == \"F4\":\n        # 클립보드1 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip1_entry.get())\n        # 클립보드1 내용 삭제\n        clip1_entry.delete(0, tk.END)\n    # F5 키를 누른 경우\n    elif key.keysym == \"F5\":\n        # 클립보드2 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip2_entry.get())\n        # 클립보드2 내용 삭제\n        clip2_entry.delete(0, tk.END)\n    # F6 키를 누른 경우\n    elif key.keysym == \"F6\":\n        # 클립보드3 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip3_entry.get())\n        # 클립보드3 내용 삭제\n        clip3_entry.delete(0, tk.END)\n\n    # display_entry를 입력불가능한 상태로 전환\n    display_entry.configure(state=\"readonly\")\n\n# 버튼 클릭시 실행 함수\ndef buttonClick(key):\n    # display_entry를 입력가능한 상태로 전환\n    display_entry.configure(state=tk.NORMAL)\n\n    # '=' 버튼을 누른 경우\n    if key == '=':\n        try:\n            # display_entry 수식을 계산\n            result = str(round(eval(display_entry.get()), 2))\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # display_entry에 결과값 출력\n            display_entry.insert(tk.END, result)\n        # 계산이 되지 않는 수식인 경우 1초 동안 '오류' 표시\n        except:\n            # 현재 display_entry 수식을 임시로 저장\n            result_tmp = display_entry.get()\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 안내 메시지 표출\n            display_entry.insert(0, \"계산할 수 없는 수식입니다\")\n            display_entry.update()\n            # 1초간 정지\n            time.sleep(1)\n            # display_entry 내용 지우기\n            display_entry.delete(0, tk.END)\n            # 임시로 저장해두었던 수식 다시 보여주기\n            display_entry.insert(0, result_tmp)\n    # 'C' 버튼을 누른 경우\n    elif key == 'C':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n    # 'AC' 버튼을 누른 경우\n    elif key == 'AC':\n        # display_entry 내용 삭제\n        display_entry.delete(0, tk.END)\n        # clip1,2,3_entry 내용 삭제\n        clip1_entry.delete(0, tk.END)\n        clip2_entry.delete(0, tk.END)\n        clip3_entry.delete(0, tk.END)\n    # '=', 'C', 'AC' 외의 버튼을 누른 경우\n    else:\n        # 눌러진 버튼 텍스트 값을 디스플레이에 추가\n        display_entry.insert(tk.END, key)\n\n    # display_entry를 입력불가능한 상태로 전환\n    display_entry.configure(state=\"readonly\")\n\n# 펑션키 입력 함수\ndef funcClick(key):\n    # display_entry를 입력가능한 상태로 전환\n    display_entry.configure(state=tk.NORMAL)\n\n    # F1 키를 누른 경우\n    if key == \"F1\":\n        # 클립보드1 내용 삭제\n        clip1_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드1로 복사\n        clip1_entry.insert(tk.END, display_entry.get())\n    # F2 키를 누른 경우\n    elif key == \"F2\":\n        # 클립보드2 내용 삭제\n        clip2_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드2로 복사\n        clip2_entry.insert(tk.END, display_entry.get())\n    # F3 키를 누른 경우\n    elif key == \"F3\":\n        # 클립보드3 내용 삭제\n        clip3_entry.delete(0, tk.END)\n        # display_entry 내용을 클립보드3로 복사\n        clip3_entry.insert(tk.END, display_entry.get())\n    # F4 키를 누른 경우\n    elif key == \"F4\":\n        # 클립보드1 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip1_entry.get())\n        # 클립보드1 내용 삭제\n        clip1_entry.delete(0, tk.END)\n    # F5 키를 누른 경우\n    elif key == \"F5\":\n        # 클립보드2 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip2_entry.get())\n        # 클립보드2 내용 삭제\n        clip2_entry.delete(0, tk.END)\n    # F6 키를 누른 경우\n    elif key == \"F6\":\n        # 클립보드3 내용을 display_entry에 추가\n        display_entry.insert(tk.END, clip3_entry.get())\n        # 클립보드3 내용 삭제\n        clip3_entry.delete(0, tk.END)\n\n    # display_entry를 입력불가능한 상태로 전환\n    display_entry.configure(state=\"readonly\")\n\n# 메인 윈도우 생성\nmain_win = tk.Tk()\n# 메인 윈도우 이름 설정\nmain_win.title(\"DIY 계산기\")\n# 메인 윈도우를 항상 위로\nmain_win.attributes(\"-topmost\", True)\n# focus-on 상태에서 키를 누르면 inputKey 함수 실행\nmain_win.bind(\"<Key>\", inputKey)\n\n# 디스플레이 엔트리 생성 및 배치\n# columnspan=2 추가\ndisplay_entry = tk.Entry(main_win, width=35, readonlybackground=\"light green\", bg=\"light green\")\ndisplay_entry.grid(row=0, column=0, columnspan=2)\n\n# display_entry를 입력불가능한 상태로 전환\ndisplay_entry.configure(state=\"readonly\")\n\n# 번호 프레임 생성 및 배치\nnum_frame = tk.Frame(main_win)\nnum_frame.grid(row=1, column=0, sticky='W')\n\n# 반복문에서 사용할 임시 변수\nrow_tmp = 0\ncol_tmp = 0\n\n# num_text는 num_list 값을 순서대로 갖게 된다\nfor num_text in num_list:\n\n    # 버튼을 눌렀을 때 cmd_tmp가 실행된다\n    def cmd_tmp(key_input=num_text):\n        # num_text 값을 넘겨주면서 button_click을 호출\n        buttonClick(key_input)\n\n    # Button을 생성하고, row_tmp, col_tmp 위치에 배치\n    num_button = tk.Button(num_frame, text=num_text, width=3, command=cmd_tmp)\n    num_button.grid(row=row_tmp, column=col_tmp)\n\n    # 버튼을 배치했으면 다음 칸으로 이동하도록 col_tmp에 1 더한다\n    col_tmp = col_tmp + 1\n    # col_tmp가 2보다 크다면 다음 줄로 이동한다\n    if col_tmp > 2:\n        col_tmp = 0\n        row_tmp = row_tmp + 1\n\n# 연산자 프레임 생성 및 배치\nop_frame = tk.Frame(main_win)\nop_frame.grid(row=1, column=1, sticky='E')\n\n# 반복문에서 사용할 임시 변수\nrow_tmp = 0\ncol_tmp = 0\n\n# op_text는 op_list 값을 순서대로 갖게 된다\nfor op_text in op_list:\n\n    # 버튼을 눌렀을 때 cmd_tmp가 실행된다\n    def cmd_tmp(key_input=op_text):\n        # op_text 값을 넘겨주면서 button_click을 호출\n        buttonClick(key_input)\n\n    # Button을 생성하고, row_tmp, col_tmp 위치에 배치\n    op_button = tk.Button(op_frame, text=op_text, width=3, command=cmd_tmp)\n    op_button.grid(row=row_tmp, column=col_tmp)\n\n    # 버튼을 배치했으면 다음 칸으로 이동하도록 col_tmp에 1 더한다\n    col_tmp = col_tmp + 1\n    # col_tmp가 1보다 크다면 다음 줄로 이동한다\n    if col_tmp > 1:\n        col_tmp = 0\n        row_tmp = row_tmp + 1\n\n# 클립보드 프레임 생성 및 배치\nclip_frame = tk.Frame(main_win)\nclip_frame.grid(row=2, column=0, columnspan=2, sticky='N')\n\n# F1~F6를 눌렀을 때 실행되는 함수\ndef cmd_F1():\n    # 단순히 funcClick을 호출\n    funcClick('F1')\n\ndef cmd_F2():\n    # 단순히 funcClick을 호출\n    funcClick('F2')\n\ndef cmd_F3():\n    # 단순히 funcClick을 호출\n    funcClick('F3')\n\ndef cmd_F4():\n    # 단순히 funcClick을 호출\n    funcClick('F4')\n\ndef cmd_F5():\n    # 단순히 funcClick을 호출\n    funcClick('F5')\n\ndef cmd_F6():\n    # 단순히 funcClick을 호출\n    funcClick('F6')\n\n# 클립보드1 입출력 버튼, 엔트리 생성 및 배치\nclip1_input_btn = tk.Button(clip_frame, width=2, text='F1', command=cmd_F1)\nclip1_input_btn.grid(row=0, column=0)\nclip1_entry = tk.Entry(clip_frame, width=20, bg=\"light pink\")\nclip1_entry.grid(row=0, column=1)\nclip1_output_btn = tk.Button(clip_frame, width=2, text='F4', command=cmd_F4)\nclip1_output_btn.grid(row=0, column=2)\n\n# 클립보드2 입출력 버튼, 엔트리 생성 및 배치\nclip2_input_btn = tk.Button(clip_frame, width=2, text='F2', command=cmd_F2)\nclip2_input_btn.grid(row=1, column=0)\nclip2_entry = tk.Entry(clip_frame, width=20, bg=\"light blue\")\nclip2_entry.grid(row=1, column=1)\nclip2_output_btn = tk.Button(clip_frame, width=2, text='F5', command=cmd_F5)\nclip2_output_btn.grid(row=1, column=2)\n\n# 클립보드3 입출력 버튼, 엔트리 생성 및 배치\nclip3_input_btn = tk.Button(clip_frame, width=2, text='F3', command=cmd_F3)\nclip3_input_btn.grid(row=2, column=0)\nclip3_entry = tk.Entry(clip_frame, width=20, bg=\"light yellow\")\nclip3_entry.grid(row=2, column=1)\nclip3_output_btn = tk.Button(clip_frame, width=2, text='F6', command=cmd_F6)\nclip3_output_btn.grid(row=2, column=2)\n\n# 메인 윈도우 반복 명령\nmain_win.mainloop()",
      "validation": {
        "type": "multi-line-exact",
        "allowTrim": true,
        "allowExtraBlankLines": true,
        "editMode": "modify"
      },
      "commonErrors": [
        {
          "pattern": "display_entry.delete(display_len, tk.END)",
          "type": "IndexError",
          "message": "인덱스는 0부터 시작한다. 마지막 글자의 위치는 `글자수 - 1`이다. `display_len-1`을 사용해야 한다."
        },
        {
          "pattern": "elif key.keysym == \"BackSpace\":",
          "type": "LogicError",
          "message": "BackSpace 블록은 char 블록과 독립적인 `if`로 시작해야 한다. `elif`로 연결하면 char 조건 중 하나가 실행된 경우 BackSpace가 무시된다."
        },
        {
          "pattern": "display_entry.delete(display_len-1, display_len)",
          "type": "TypeError",
          "message": "두 번째 인수로 `tk.END`를 사용해야 한다. `display_len`은 정수라서 위젯의 끝을 나타내는 tk.END와 다르다."
        }
      ],
      "successMessage": "Lesson 4 완성! 백스페이스로 마지막 글자를 지울 수 있어요. 계산기가 완성됐습니다!",
      "mascot": "고드래곤_05.jpg",
      "mascotSpeech": "백스페이스로 한 글자씩 지울 수 있게 됐어! delete(시작, 끝)에서 인덱스는 0부터 시작하니까 마지막 글자는 len-1이야. 그리고 BackSpace도 keysym으로 감지하는데, F키 블록이랑 별개로 독립된 if야. Lesson 4 완성 축하해!!",
      "completionBadges": ["Lesson 4 완성자"]
    }
  ],
  "lessonCompletion": {
    "title": "Lesson 4 완성!",
    "summary": "클립보드 기능, 펑션키 연결, 백스페이스 지우기를 추가해 계산기를 완성했습니다.",
    "summaryTemplate": "계산기 Lesson 4를 {elapsedTime} 만에 완성했어요. 실행 {runCount}회, 오타 해결 {typoCount}회, 최고 연속 성공 {bestSuccessStreak}회를 기록했습니다."
  }
}
